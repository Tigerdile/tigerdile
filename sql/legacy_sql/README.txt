These are "legacy" SQL files.  All of these have been applied to the base DB, so there is no need to ever run them again.  I've kept them around for historical reference as it has some useful SQL in them.


-- sconley

